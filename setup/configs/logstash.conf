input {
  beats {
    port => 5044
  }
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    index => "dealership"
    cacert => "C:/apps/logstash-8.0.0/config/certs/http_ca.crt"
    user => "elastic"
    password => ""
  }
}
